@model List<BookCave.Models.ViewModels.OrderListViewModel>
@{

}
<!-- List of all books in cart: -->
<div id="booksInCart">
  <div>
       @foreach(var book in Model) {
        <div class="OneItemInCart">
            <div class="addToButtons">
                <button type="button" class="wishListButton"><i class="far fa-heart"></i></button>
            </div>
        </div>

        <div id="Price">
          Price: 
        </div>

        <!-- SKOÐA BETUR ÞESSA SLÓÐ: https://css-tricks.com/number-increment-buttons/ :-->
        <div id="Quantity">
          <form method="post" action="">
              <label for="Quantity">Quantity</label>
              <input type="text" name="Quantity" id="quantiy-box" value="1">
          </form>
        </div>

        <div class="deleteButton">
            <button type="button"></button>
        </div>
      }
  </div>
</div>