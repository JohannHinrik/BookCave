@model Tuple<BookCave.Models.ViewModels.BookListViewModel,List<BookCave.Models.ViewModels.ReviewListModel>>

<div class ="row">
    <h2>@Model.Item1.Title</h2>
    <div class="col-xs-6 col-lg-6">
        <div>
            <img src="~/images/books/(@Model.BookId).jpg" alt="@Model.Title" class="detailBookCover">
        </div>
    </div>
    <div class="col-xs-6 col-lg-6">
        Genre: @Model.Item1.Genre <br/>
        Author: @Model.Item1.Author <br/>
        About: <br/>
        @Model.Item1.About
        <br/>
        <div class="addToButtons">
            <h4><span>@Model.Item1.Price Kr</span></h4>
            @for(var i = Math.Round(@Model.Item1.Rating); i > 0; i--)
            {
                <i id="yes" class="fas fa-star"></i>
            }
            @for(var i = Math.Round(@Model.Item1.Rating); i < 5; i++)
            {
                <i id="no" class="fas fa-star"></i>
            }
            <button type="button" class="cartButton">Add to Cart</button>
            <br/>
            <button type="button" class="wishListButton"><i class="far fa-heart"></i></button>
        </div>
    </div>

</div>



<div class="col-xs-6 col-lg-6">
    <!-- ASK ABOUT THIS (COMENTS) -->     
    <h4>Leave a Comment:</h4>

    <form role="form">
        <div class="form-group">
            <textarea class="form-control" rows="3" required></textarea>
        </div>
        <button type="submit" class="btn btn-success">Submit</button>
    </form>

    <br><br>

    <h4><small>RECENT POSTS</small></h4>
    <hr>
    <div class="ListOfComments">
        <hr>
        <!-- @foreach(var review in Model.Item1)
        {
            username: review.Username
            <div id="OneComment">
                comment: review.Comment
            </div>
            <hr>
        } -->

    </div>
</div>